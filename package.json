{
  "name": "env-verifier-cli",
  "version": "1.1.3",
  "bin": {
    "env-verifier": "./bin/index.js"
  },
  "scripts": {
    "test": "jest --verbose"
  },
  "keywords": [
    "env-verifier-cli",
    "env-cli-validate",
    "env-validate",
    "env-checker"
  ],
  "repository": {
    "type": "git",
    "url": "https://github.com/tutyamxx/env-verifier-cli"
  },
  "author": "tuty",
  "license": "MIT",
  "type": "commonjs",
  "description": "🛡️ A command-line tool to validate .env files against a defined schema to ensure correct environment variable configurations.",
  "dependencies": {
    "chalk": "^4.1.2",
    "commander": "^14.0.3",
    "date-fns": "^4.1.0",
    "dotenv": "^17.3.1",
    "dotenv-expand": "^12.0.2"
  },
  "devDependencies": {
    "jest": "^30.2.0"
  }
}
